#!/usr/bin/env python3
"""Launcher for the spotmv CLI (see src/spotmv/)."""

import sys
from pathlib import Path

sys.path.insert(0, str(Path(__file__).resolve().parent / "src"))

from spotmv.cli import main  # noqa: E402  (path setup must happen first)

raise SystemExit(main())
